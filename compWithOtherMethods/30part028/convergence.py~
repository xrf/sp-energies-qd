import matplotlib.pyplot as plt
import numpy as np
import linecache as lc
import sys
import os
from pylab import *

standard = loadtxt("standard.dat")
effective = loadtxt("effective.dat")
standardCCSD = loadtxt("standardCCSD.dat")
effectiveCCSD = loadtxt("effectiveCCSD.dat")

fig = plt.figure(figsize=plt.figaspect(1.))
#ax = fig.add_subplot(2, 1, 1)
ax = fig.gca()
ax.set_xlim((8,20))
ax.set_ylabel(r'$E_0\, [E_H]$',fontsize=18)
ax.set_xlabel(r'$R$',fontsize=18)

#ax.set_ylim((308,319))

l = len(standard[:,0])
data7 = zeros(l)
data7[:] = 123.9683

plt.plot(standard[:,0], standard[:,2],'b', standard[:,0], standard[:,1],'r-o', standardCCSD[:,0], standardCCSD[:,1], 'm*-',standard[:,0], data7[:], 'k--')

#plt.title(r'$N=20,\, \omega=1.0,\, V_{std}$', fontsize=16, horizontalalignment='center')
plt.legend(('SRG','HF','CCSD','DMC'),
           'upper right', shadow=False, fancybox=False)

plt.show()
